import { StyleSheet } from "react-native";

/**
 * Styles for ConfigurableQuestionnaire component
 */
export const configurableQuestionnaireStyles = StyleSheet.create({
  scrollViewContent: {
    paddingBottom: 10,
  },
  textInput: {
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 8,
    padding: 12,
    fontSize: 16,
    backgroundColor: "#fff",
  },
});
