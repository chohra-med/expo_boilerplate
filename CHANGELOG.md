# Changelog

All notable changes to this boilerplate are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added

- **Wire AI integration layer, env-gated OFF by default.** Copied the app-agnostic shared layer
  from the Standard tier (source of truth) verbatim — `src/services/wire/` (the single
  `EXPO_PUBLIC_WIREAI_*` read + derived config, the fail-closed gate-storage factory, the
  permission-screen builder, the report-only RevenueCat → Wire purchase funnel),
  `src/ui/providers/wire-provider.tsx` (feature flags + icon registry), and
  `src/ui/hooks/use-wire-questionnaire-gate.ts`. Mounted per this repo's structure:
  `WireProvider` at the app root, `<WireQuestionnaireGate />` on the home screen,
  `permissionScreens` on the onboarding flow, a `__DEV__`-only `<WireDemoOnboarding />` in
  settings, and `createWirePurchaseFunnel` reporting the five purchase moments on the paywall.
  With no Wire key set (a fresh clone) every surface is inert — no crash, no network, no console
  noise — proven by `src/services/wire/__tests__/wire-no-env-inert.test.tsx`. No new runtime
  dependency: all imports resolve to packages already in `package.json`. The permission-screen
  mount ships EMPTY (this template bundles no permissions module) with a documented injection
  point. See `src/features/wire/README.md`.

### Changed

- Bumped `@wireai/activation` `0.15.0` → **`0.16.0`** (2026-08-20), pin style
  unchanged (exact, no caret) with `yarn.lock` regenerated by `yarn add --exact` in
  the same commit — the lockfile is the only thing that decides what actually
  installs. `wireai-rn` stays at `0.2.5`; no other dependency moved, and the
  lockfile churn is the single `@wireai/activation` entry (resolves
  `activation-0.16.0.tgz#81cc08be1ed4d74a36811562170c7667921503fa`).

  **Nothing was removed or renamed — proven, not assumed.** Both tarballs were
  unpacked and compared: `exports`, `dependencies`, `peerDependencies`,
  `peerDependenciesMeta` and `sideEffects` are byte-identical across the two
  manifests, and the exported symbol lists of the root, `coachmarks` and `reviews`
  entry points are unchanged. The MINOR is earned by exactly ONE gained public
  type on the `analytics` barrel — `WireDoctorJoinTarget` — plus the optional
  `join` field it types on `WireDoctorOptions`. **No source file in this template
  changes.**

  - **The behaviour change to know about is `wireDoctor`, and it costs this
    template nothing.** `join` is optional to the type-checker but omitting it now
    FAILS the report's `join_key` check by design ("a report that never looked at
    the join cannot honestly read green"). Re-grepped at this commit: there is **no
    `wireDoctor` call site under `src/`** — the only mentions are prose in
    `CHANGELOG.md` and `EMERGENT.md`. `EMERGENT.md` step 9 handed a new integrator
    a `wireDoctor(...)` call with no `join` and told them to expect every check
    `ok`, which 0.16.0 makes unreachable, so that step now passes `join` and says
    why.
  - **Runtime fixes that land on surfaces this template mounts.** The
    questionnaire's recovery net was inert while its POST was in flight, so an
    interrupted submit lost the answers AND spent the permanent once-gate
    (`<WireQuestionnaireGate />` on the home screen, `useQuestionnaireGate` in
    `src/ui/hooks/use-wire-questionnaire-gate.ts`); a minted session id counted as
    a second app-open, so a review prompt could fire inside a user's FIRST session;
    the gate counter stalled on a screen that never unmounts; `fetchWireFeatures`
    had no ceiling on its body read, so both gates could hold dark on RN-Web;
    StrictMode handed the host a disposed analytics instance (dev-only silent
    no-op `track()`); and five awaited fetches gained a deadline.
  - The review-gate half of that list is inert here: this template still mounts no
    review gate — nothing under `src/` imports `@wireai/activation/reviews`.
  - Verified locally on an isolated worktree off `development` (`f3bd503`) with its
    own `node_modules`, BEFORE the edit and AFTER, on the same tree:
    `yarn type:check` exit 0 both times; `yarn lint:check` exit 1 both times with
    the SAME 1 error + 5 warnings, every one of them pre-existing and every one in
    `src/features/paywall/screens/paywall-screen.tsx`; `yarn test:ci` exit 0 both
    times, **10 suites / 84 tests passed**. Delta from this bump is ZERO.
  - Existing installs do not get this from an OTA JS update alone — the kit carries
    native peers, so it reaches users only in a NEW NATIVE BUILD.

- Bumped `@wireai/activation` `0.14.3` → **`0.15.0`** (2026-08-19), pin style
  unchanged (exact, no caret) with `yarn.lock` regenerated by `yarn install` in the
  same commit — the lockfile is the only thing that decides what actually installs.
  `wireai-rn` stays at `0.2.5`; no other dependency moved.

  **0.15.0 is additive and optional-only.** Two exported surfaces each GAINED an
  optional member (`ReviewSubmission.idempotency_key`, `buildReviewSubmission`'s
  `idempotencyKey` input); nothing was renamed or removed. That new API is the
  review gate's, and this template mounts no review gate — nothing under `src/`
  imports `@wireai/activation/reviews` — so the added surface is unused here.

  - **The one change that could have bitten this template does not.** 0.15.0 makes
    the kit's SYNC gate storage fail CLOSED when handed an ASYNC adapter (an async
    `getItem` returns a Promise, which never reads as "seen", so tours/showcases
    replayed on every open and a `minSessions` 0/1 review gate could re-fire). Every
    gate storage here is synchronous — `coachmarkStorage` wraps MMKV's `getString`
    (`src/services/storage/coachmark-storage.ts`) and `wireGateStorage` binds MMKV
    through the shared `createWireGateStorage` factory
    (`src/features/wire/services/wire-gate-storage.ts`) — so the new probe
    substitutes nothing and behaviour is unchanged.
  - No import site changed. All five `@wireai/activation` subpaths this template
    uses (root, `/analytics`, `/coachmarks`, `/showcase`, `/questionnaire`) still
    resolve, and the `selectTourSteps` mock in `src/config/coachmarks.test.ts` still
    mirrors the real contract. `yarn type:check`, `yarn lint:check` and `yarn test:ci`
    (10 suites / 84 tests) give the same result before and after the bump.
  - Existing installs do not get this from an OTA JS update alone — the kit is a
    native-peer-carrying dependency, so it reaches users in a NEW NATIVE BUILD.
  - (The earlier `0.13.6` → `0.14.3` bump shipped without a changelog entry. What
    0.14.3 brought that matters to this template — every optional peer became a
    guarded lazy require — is recorded in `EMERGENT.md`; the full range is in the
    kit's own `CHANGELOG.md`.)

- Bumped `@wireai/activation` `^0.11.0` → **`0.13.6`** and `wireai-rn` `^0.2.4` →
  **`0.2.5`** (2026-08-17), both pinned EXACTLY (no caret) with `yarn.lock`
  regenerated in the same commit. `^0.11.0` is a 0.x caret, so it resolved to
  `>=0.11.0 <0.12.0` — the pin is what makes the version readable from
  `package.json` alone.
  (The earlier `^0.10.0` → `^0.11.0` bump shipped without a changelog entry. This
  entry covers the whole `0.11.0 → 0.13.6` range.)

  **What the range brings, in order of how much it bites the default config:**
  - **0.13.3 — the loading screen could hang forever.** `maxRetries` never
    re-armed, so on the DEFAULT `maxRetries={1}` exactly one retry ever fired and
    no terminal state was reached: a backend hung at start gave a 15s loader and
    then a permanent "Getting started…", with `fallbackFlow` unreachable. This
    boilerplate's onboarding screen has always promised "a backend error degrades
    to the static flow" — that promise is only actually kept from this version on.
  - **0.13.3 — a retry could be recorded as the user's first answer.** The
    kickoff payload was keyed on a mount-time prop, so a retry re-sent
    `startMessage` and the server recorded it as the answer to the pending
    question. Only the first attempt of a fresh session sends it now. Stated
    cost: a retry SKIPS a pending card, so the user loses one question.
  - **0.12.2 / 0.13.0 — the `device_key` join key** (see Fixed below).
  - **0.13.5 — `wireDoctor`**, a dev-only integration self-check on
    `@wireai/activation/analytics`. Available, deliberately not wired here yet.
  - **0.13.6 — `OnboardingResult.plan` and `.variant`**, the backend's plan and
    the assigned experiment arm handed to the host uninterpreted. Both keys are
    absent (not `undefined`) when the backend sends neither, so `handleComplete`
    is unaffected. `wireai-rn@0.2.5` is what surfaces the extra DataPart the
    `plan` read needs; `0.2.4` simply reads no plan.
  - `DoneBlock` and `RESERVED_USER_CONTEXT_KEYS` were removed in 0.13.0. Neither
    is referenced anywhere in `src/` — verified, no action needed.
  - No import site changed. All six `@wireai/activation` subpaths this template
    uses (root, `/showcase`, `/analytics`, `/coachmarks`) still resolve, and the
    `selectTourSteps` mock in `src/config/coachmarks.test.ts` still mirrors the
    real 0.13.6 contract exactly.

### Fixed

- **The Wire onboarding storage adapter now reports failure instead of faking
  success** (`src/features/onboarding/services/wire-onboarding-storage.ts`).
  Every method used to swallow its MMKV error and resolve as if the call had
  worked. Since kit 0.13.0 that is actively harmful: the kit decides whether it
  may auto-inject `user_context.device_key` — the only thing that joins an
  onboarding session to everything the app reports later — by asking whether the
  write actually SUCCEEDED, not whether a `storage` prop was passed. An adapter
  that always reports success makes that gate inert, so a locked / full /
  permission-denied MMKV silently produces a NEW join key on every launch, which
  corrupts the server's `min_sessions` counter instead of merely leaving the join
  empty. `getItem` / `setItem` / `removeItem` now reject on a real failure; every
  kit consumer already catches, so onboarding still never breaks. Covered by
  `wire-onboarding-storage.test.ts` (8 tests; the three rejection cases fail
  against the old adapter).

- **Stale comments corrected.** `wire-analytics.ts` credited the auto-minted
  `device_key` to "(0.8.0)" and described it as unconditionally stable; stability
  has depended on the storage adapter since 0.13.0. The onboarding screen now
  documents what `storage` actually buys (session resume AND the join key) and
  why no `userContext` is passed here.

### Changed (earlier)

- Bumped `@wireai/activation` `^0.8.0` → `^0.10.0` (2026-07-19). The lockfile
  now resolves to exactly `0.10.0`. What the bump brings:
  - the kit-owned event-trigger surface `wire.track()` / `useWireActivation()`
    plus decision revalidation (kit #49);
  - a cross-bundle session singleton
    (`globalThis[Symbol.for("@wireai/activation:currentSessionId")]`) so
    `getCurrentSessionId()` is reliable when the kit runs from `dist`;
  - the 0.9.2 `retainSessionOnComplete` funnel-seed fix (one signup = one
    `session_started`);
  - the 0.9.1 review-decision truth fixes.
  - No consumer code changes required: every symbol this template imports
    (`WireOnboarding`, `wireConfigFromEnv`, `isOnboardingEnabled`,
    `WireOnboardingStorage`, `OnboardingResult`, `OnboardingEvent`, and the
    `@wireai/activation/showcase` + `@wireai/activation/coachmarks` subpath
    exports) is unchanged in 0.10.0. Typecheck, the Jest suite, and the Expo JS
    bundle all pass on the bump.
  - Not adopted in this pass: the new `wire.track()` event API is available but
    intentionally left unwired here (a separate, deliberate follow-up).
